# Workflow: package-extension.yml
# Packages the Chrome extension into a zip and uploads as an artifact.
# If you push a tag (refs/tags/v*), it will create a GitHub Release and attach the zip.
on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js (for optional build)
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies (if package.json exists)
        run: |
          if [ -f package.json ]; then
            npm ci
          else
            echo "no package.json, skipping npm install"
          fi

      - name: Run build (if npm run build exists)
        run: |
          if [ -f package.json ]; then
            if npm run | grep -q 'build'; then
              npm run build
            else
              echo "no build script, skipping build"
            fi
          else
            echo "no package.json, skipping build"
          fi

      - name: Prepare package directory
        run: |
          set -e
          PKG_DIR=package
          ROOT_NAME="BiliQueue-${{ github.run_id }}"
          ROOT_DIR="$PKG_DIR/$ROOT_NAME"
          rm -rf "$PKG_DIR"
          mkdir -p "$ROOT_DIR"

          # copy manifest.json (expected at repo root)
          if [ -f manifest.json ]; then
            cp manifest.json "$ROOT_DIR/"
          else
            echo "WARNING: manifest.json not found at repo root"
          fi

          # Prefer `dist/` (common build output); if not present, copy `src/`.
          if [ -d dist ]; then
            rsync -a dist/ "$ROOT_DIR/"
          elif [ -d build ] && [ "$(ls -A build)" ]; then
            rsync -a build/ "$ROOT_DIR/"
          elif [ -d src ]; then
            # preserve src/ folder at package root
            rsync -a src "$ROOT_DIR/"
          else
            echo "No dist/build/src found — copying repository files except .git and node_modules"
            rsync -a --exclude='.git' --exclude='node_modules' ./ "$ROOT_DIR/"
          fi

          # copy icons if any
          if [ -d icons ]; then
            rsync -a icons "$ROOT_DIR/" || true
          fi

      - name: Create zip (Chrome extension zip)
        run: |
          set -e
          OUT_NAME="BiliQueue-${{ github.run_id }}.zip"
          cd package
          # Zip the root folder into one zip
          zip -r "../${OUT_NAME}" "BiliQueue-${{ github.run_id }}"
          cd ..
          ls -lh "${OUT_NAME}"

      - name: Upload packaged zip as artifact
        uses: actions/upload-artifact@v4
        with:
          name: biliqueue-extension
          path: BiliQueue-*.zip

      - name: Create GitHub Release and attach zip (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.ref_name }}
          artifacts: BiliQueue-*.zip 
          token: ${{ secrets.GITHUB_TOKEN }}
